<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.gateway.persistence.dao.RouterVersionDao">

  <resultMap id="RouterVersionEntityMap"
    type="pwd.initializr.gateway.persistence.entity.RouterVersionEntity">
    <result column="version" jdbcType="BIGINT" property="version"/>
    <result column="create_time" jdbcType="VARCHAR" property="createTime"/>
  </resultMap>

  <select id="createTable">
    CREATE TABLE IF NOT EXISTS router_version(
     version       INT PRIMARY KEY     NOT NULL,
     create_time   CHAR(20)            NOT NULL
    );
  </select>

  <!--查询-->
  <select id="query" resultMap="RouterVersionEntityMap">
    SELECT version, create_time FROM router_version;
  </select>

  <!--删除-->
  <delete id="delete">
    DELETE FROM router_version;
  </delete>

  <!--新增-->
  <insert id="create" keyColumn="serial_number" keyProperty="serialNumber"
    parameterType="pwd.initializr.gateway.persistence.entity.RouterVersionEntity">
    INSERT INTO router_version(version,create_time)
    VALUES (#{version},#{createTime});
  </insert>


</mapper>
