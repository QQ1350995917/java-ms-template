<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.monitor.persistence.dao.HostWhoDao">

  <resultMap id="HostWhoMap" type="pwd.initializr.monitor.persistence.entity.HostWhoEntity">
    <result column="id" jdbcType="VARCHAR" property="id"/>
    <result column="user" jdbcType="VARCHAR" property="user"/>
    <result column="device" jdbcType="VARCHAR" property="device"/>
    <result column="host" jdbcType="VARCHAR" property="host"/>
    <result column="time" jdbcType="BIGINT" property="time"/>
    <result column="able" jdbcType="INTEGER" property="able"/>
    <result column="del" jdbcType="INTEGER" property="del"/>
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
  </resultMap>

  <!--根据主键查询查询-->
  <select id="queryById" resultMap="HostWhoMap">
    SELECT
      `id`,
      `user`,
      `device`,
      `host`,
      `time`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      host_who
    WHERE
      id = #{id}
  </select>

  <!--查询指定行数据-->
  <select id="countByCondition" resultType="java.lang.Long">
    SELECT COUNT(*) FROM host_who
    <include refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryByCondition" resultMap="HostWhoMap">
    SELECT
      `id`,
      `user`,
      `device`,
      `host`,
      `time`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      host_who
    <include refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
    <include refid="pwd.initializr.common.web.persistence.entity.SortEntity.entityQuerySort"></include>
    LIMIT
      #{offset}, #{limit}
  </select>

  <!--新增所有列-->
  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
    INSERT INTO host_who
      (
        `id`,
        `user`,
        `device`,
        `host`,
        `time`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
      )
    VALUES
      (
        #{entity.id},
        #{entity.user},
        #{entity.device},
        #{entity.host},
        #{entity.time},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
      )
  </insert>

  <!--新增所有列-->
  <insert id="insertBatch" >
    INSERT INTO host_who
      (
        `id`,
        `user`,
        `device`,
        `host`,
        `time`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
      )
    VALUES
    <foreach collection="entities" item="entity" separator=",">
      (
      #{entity.id},
      #{entity.user},
      #{entity.device},
      #{entity.host},
      #{entity.time},
      #{entity.able},
      #{entity.del},
      #{entity.createTime},
      #{entity.updateTime}
      )
    </foreach>
  </insert>

  <!--通过主键修改数据-->
  <update id="updateById">
    UPDATE host_who
    <set>
      <if test="entity.id != null and entity.id != ''">
        `id` = #{entity.id},
      </if>
      <if test="entity.user != null and entity.user != ''">
        `user` = #{entity.user},
      </if>
      <if test="entity.device != null and entity.device != ''">
        `device` = #{entity.device},
      </if>
      <if test="entity.host != null and entity.host != ''">
        `host` = #{entity.host},
      </if>
      <if test="entity.time != null and entity.time != ''">
        `time` = #{entity.time},
      </if>
      <if test="entity.able != null and entity.able != ''">
        `able` = #{entity.able},
      </if>
      <if test="entity.del != null and entity.del != ''">
        `del` = #{entity.del},
      </if>
      <if test="entity.createTime != null and entity.createTime != ''">
        `create_time` = #{entity.createTime},
      </if>
      <if test="entity.updateTime != null and entity.updateTime != ''">
        `update_time` = #{entity.updateTime},
      </if>
    </set>
    WHERE
      id = #{entity.id}
  </update>

  <!--通过主键删除-->
  <update id="deleteById">
    UPDATE host_who
    SET
      del = 1,
      update_time = now()
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量删除-->
  <update id="deleteByIds">
    UPDATE host_who
    SET
      del = 1,
      update_time = now()
    WHERE
      id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

  <!--通过主键启禁-->
  <update id="ableById">
    UPDATE host_who
    SET
      able = #{able},
      update_time = now()
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量启禁-->
  <update id="ableByIds">
    UPDATE host_who
    SET
      able = #{able},
      update_time = now()
    WHERE
      id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

</mapper>
