<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.monitor.persistence.dao.HostEthernetDao">

  <resultMap id="HostEthernetMap" type="pwd.initializr.monitor.persistence.entity.HostEthernetEntity">
    <result column="id" jdbcType="VARCHAR" property="id"/>
    <result column="hwaddr" jdbcType="VARCHAR" property="hwaddr"/>
    <result column="name" jdbcType="VARCHAR" property="name"/>
    <result column="type" jdbcType="VARCHAR" property="type"/>
    <result column="description" jdbcType="VARCHAR" property="description"/>
    <result column="address" jdbcType="VARCHAR" property="address"/>
    <result column="destination" jdbcType="VARCHAR" property="destination"/>
    <result column="broadcast" jdbcType="VARCHAR" property="broadcast"/>
    <result column="netmask" jdbcType="VARCHAR" property="netmask"/>
    <result column="flags" jdbcType="BIGINT" property="flags"/>
    <result column="mtu" jdbcType="BIGINT" property="mtu"/>
    <result column="metric" jdbcType="BIGINT" property="metric"/>
    <result column="able" jdbcType="INTEGER" property="able"/>
    <result column="del" jdbcType="INTEGER" property="del"/>
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
  </resultMap>

  <!--根据主键查询查询-->
  <select id="queryById" resultMap="HostEthernetMap">
    SELECT
      `id`,
      `hwaddr`,
      `name`,
      `type`,
      `description`,
      `address`,
      `destination`,
      `broadcast`,
      `netmask`,
      `flags`,
      `mtu`,
      `metric`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      host_ethernet
    WHERE
      id = #{id}
  </select>

  <!--查询指定行数据-->
  <select id="countByCondition" resultType="java.lang.Long">
    SELECT COUNT(*) FROM host_ethernet
    <include refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryByCondition" resultMap="HostEthernetMap">
    SELECT
      `id`,
      `hwaddr`,
      `name`,
      `type`,
      `description`,
      `address`,
      `destination`,
      `broadcast`,
      `netmask`,
      `flags`,
      `mtu`,
      `metric`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      host_ethernet
    <include refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
    <include refid="pwd.initializr.common.web.persistence.entity.SortEntity.entityQuerySort"></include>
    LIMIT
      #{offset}, #{limit}
  </select>

  <!--新增所有列-->
  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
    INSERT INTO host_ethernet
      (
        `id`,
        `hwaddr`,
        `name`,
        `type`,
        `description`,
        `address`,
        `destination`,
        `broadcast`,
        `netmask`,
        `flags`,
        `mtu`,
        `metric`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
      )
    VALUES
      (
        #{entity.id},
        #{entity.hwaddr},
        #{entity.name},
        #{entity.type},
        #{entity.description},
        #{entity.address},
        #{entity.destination},
        #{entity.broadcast},
        #{entity.netmask},
        #{entity.flags},
        #{entity.mtu},
        #{entity.metric},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
      )
  </insert>

  <!--新增所有列-->
  <insert id="insertOrReplace" keyProperty="id" useGeneratedKeys="true">
    REPLACE INTO host_ethernet
      (
        `id`,
        `hwaddr`,
        `name`,
        `type`,
        `description`,
        `address`,
        `destination`,
        `broadcast`,
        `netmask`,
        `flags`,
        `mtu`,
        `metric`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
      )
    VALUES
      (
        #{entity.id},
        #{entity.hwaddr},
        #{entity.name},
        #{entity.type},
        #{entity.description},
        #{entity.address},
        #{entity.destination},
        #{entity.broadcast},
        #{entity.netmask},
        #{entity.flags},
        #{entity.mtu},
        #{entity.metric},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
      )
  </insert>

  <!--通过主键修改数据-->
  <update id="updateById">
    UPDATE host_ethernet
    <set>
      <if test="entity.id != null and entity.id != ''">
        `id` = #{entity.id},
      </if>
      <if test="entity.hwaddr != null and entity.hwaddr != ''">
        `hwaddr` = #{entity.hwaddr},
      </if>
      <if test="entity.name != null and entity.name != ''">
        `name` = #{entity.name},
      </if>
      <if test="entity.type != null and entity.type != ''">
        `type` = #{entity.type},
      </if>
      <if test="entity.description != null and entity.description != ''">
        `description` = #{entity.description},
      </if>
      <if test="entity.address != null and entity.address != ''">
        `address` = #{entity.address},
      </if>
      <if test="entity.destination != null and entity.destination != ''">
        `destination` = #{entity.destination},
      </if>
      <if test="entity.broadcast != null and entity.broadcast != ''">
        `broadcast` = #{entity.broadcast},
      </if>
      <if test="entity.netmask != null and entity.netmask != ''">
        `netmask` = #{entity.netmask},
      </if>
      <if test="entity.flags != null and entity.flags != ''">
        `flags` = #{entity.flags},
      </if>
      <if test="entity.mtu != null and entity.mtu != ''">
        `mtu` = #{entity.mtu},
      </if>
      <if test="entity.metric != null and entity.metric != ''">
        `metric` = #{entity.metric},
      </if>
      <if test="entity.able != null and entity.able != ''">
        `able` = #{entity.able},
      </if>
      <if test="entity.del != null and entity.del != ''">
        `del` = #{entity.del},
      </if>
      <if test="entity.createTime != null and entity.createTime != ''">
        `create_time` = #{entity.createTime},
      </if>
      <if test="entity.updateTime != null and entity.updateTime != ''">
        `update_time` = #{entity.updateTime},
      </if>
    </set>
    WHERE
      id = #{id}
  </update>

  <!--通过主键删除-->
  <update id="deleteById">
    UPDATE host_ethernet
    SET
      del = 1,
      update_time = now()
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量删除-->
  <update id="deleteByIds">
    UPDATE host_ethernet
    SET
      del = 1,
      update_time = now()
    WHERE
      id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

  <!--通过主键启禁-->
  <update id="ableById">
    UPDATE host_ethernet
    SET
      able = #{able},
      update_time = now()
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量启禁-->
  <update id="ableByIds">
    UPDATE host_ethernet
    SET
      able = #{able},
      update_time = now()
    WHERE
      id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

</mapper>
