<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.account.persistence.mapper.EntranceMapper">
  <!-- findSignUpByUser -->
	<select id="findUserEntranceByType" parameterType="java.lang.Integer"
		resultType="pwd.initializr.account.persistence.dao.EntranceEntity">
		select
		`id` as `id`,
		`key` as `key`,
		`name` as `name`,
		`logo` as `logo`,
		`type` as `type`,
		`status` as `status`,
		`create_time` as createTime,
		`update_time` as updateTime
		from `entrance`
		where `type` = #{type}
		and  `status` = 0;
	</select>

</mapper>