<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.account.persistence.dao.UserUserDao">

  <resultMap id="UserUserMap" type="pwd.initializr.account.persistence.entity.UserUserEntity">
    <result column="id" jdbcType="INTEGER" property="id"/>
    <result column="pin" jdbcType="VARCHAR" property="pin"/>
    <result column="name" jdbcType="VARCHAR" property="name"/>
    <result column="gender" jdbcType="VARCHAR" property="gender"/>
    <result column="emp_no" jdbcType="VARCHAR" property="empNo"/>
    <result column="summary" jdbcType="VARCHAR" property="summary"/>
    <result column="able" jdbcType="INTEGER" property="able"/>
    <result column="del" jdbcType="INTEGER" property="del"/>
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
  </resultMap>

  <!--查询单个-->
  <select id="queryById" resultMap="UserUserMap">
        select
          id, pin, name, gender, emp_no, summary, able, del, create_time, update_time
        from initializr_account.user_user
        where id = #{id}
    </select>


  <!--查询指定行数据-->
  <select id="countAllByCondition" resultType="java.lang.Long">
    select count(*) from initializr_account.user_user
    <include refid="countAndQueryAllByCondition"></include>
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryAllByCondition" resultMap="UserUserMap">
    select
    id, pin, name, gender, emp_no, summary, able, del, create_time, update_time
    from initializr_account.user_user
    <include refid="countAndQueryAllByCondition"></include>
    limit #{offset}, #{limit}
  </select>

  <!-- 统计和查询条件 -->
  <sql id="countAndQueryAllByCondition">
    <where>
      <if test="userUserEntity.id != null">
        and id = #{userUserEntity.id}
      </if>
      <if test="userUserEntity.pin != null and userUserEntity.pin != ''">
        and pin = #{userUserEntity.pin}
      </if>
      <if test="userUserEntity.name != null and userUserEntity.name != ''">
        and name = #{userUserEntity.name}
      </if>
      <if test="userUserEntity.gender != null and userUserEntity.gender != ''">
        and gender = #{userUserEntity.gender}
      </if>
      <if test="userUserEntity.empNo != null and userUserEntity.empNo != ''">
        and emp_no = #{userUserEntity.empNo}
      </if>
      <if test="userUserEntity.summary != null and userUserEntity.summary != ''">
        and summary = #{userUserEntity.summary}
      </if>
      <if test="userUserEntity.able != null">
        and able = #{userUserEntity.able}
      </if>
      <if test="userUserEntity.del != null">
        and del = #{userUserEntity.del}
      </if>
      <if test="userUserEntity.createTime != null">
        and create_time = #{userUserEntity.createTime}
      </if>
      <if test="userUserEntity.updateTime != null">
        and update_time = #{userUserEntity.updateTime}
      </if>
    </where>
  </sql>

  <!--新增所有列-->
  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into initializr_account.user_user(pin, name, gender, emp_no, summary, able, del, create_time, update_time)
        values (#{pin}, #{name}, #{gender}, #{empNo}, #{summary}, #{able}, #{del}, #{createTime}, #{updateTime})
    </insert>

  <!--通过主键修改数据-->
  <update id="update">
    update initializr_account.user_user
    <set>
      <if test="pin != null and pin != ''">
        pin = #{pin},
      </if>
      <if test="name != null and name != ''">
        name = #{name},
      </if>
      <if test="gender != null and gender != ''">
        gender = #{gender},
      </if>
      <if test="empNo != null and empNo != ''">
        emp_no = #{empNo},
      </if>
      <if test="summary != null and summary != ''">
        summary = #{summary},
      </if>
      <if test="able != null">
        able = #{able},
      </if>
      <if test="del != null">
        del = #{del},
      </if>
      <if test="createTime != null">
        create_time = #{createTime},
      </if>
      <if test="updateTime != null">
        update_time = #{updateTime},
      </if>
    </set>
    where id = #{id}
  </update>

  <!--通过主键删除-->
  <delete id="deleteById">
    update initializr_account.user_user set del = 1, update_time = now() where id = #{id}
  </delete>

  <!--通过主键删除-->
  <delete id="deleteByIds">
    update initializr_account.user_user set del = 1, update_time = now() where id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </delete>

  <!--通过主键启禁-->
  <update id="ableById">
    update initializr_account.user_user set enable = #{able}, update_time = now() where id = #{id}
  </update>

  <!--通过主键启禁-->
  <update id="ableByIds">
    update initializr_account.user_user set enable = #{able}, update_time = now() where id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>


</mapper>
