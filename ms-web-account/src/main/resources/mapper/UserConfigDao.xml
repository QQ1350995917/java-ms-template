<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.account.persistence.dao.UserConfigDao">

  <resultMap id="UserConfigMap" type="pwd.initializr.account.persistence.entity.UserConfigEntity">
    <result column="id" jdbcType="BIGINT" property="id"/>
    <result column="key" jdbcType="VARCHAR" property="key"/>
    <result column="value" jdbcType="VARCHAR" property="value"/>
    <result column="summary" jdbcType="VARCHAR" property="summary"/>
    <result column="able" jdbcType="INTEGER" property="able"/>
    <result column="del" jdbcType="INTEGER" property="del"/>
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    <result column="version" jdbcType="BIGINT" property="version"/>
  </resultMap>

  <!--查询单个-->
  <select id="queryById" resultMap="UserConfigMap">
        select
          id, key, value,summary,able, del, create_time, update_time, version
        from initializr_account.user_config
        where id = #{id}
    </select>
  <!--查询单个-->
  <select id="queryByKey" resultMap="UserConfigMap">
        select
          id, key, value,summary,able, del, create_time, update_time
        from initializr_account.user_config
        where key = #{key}
    </select>

  <!--查询指定行数据-->
  <select id="queryAllByLimit" resultMap="UserConfigMap">
        select
          id, key, value,summary,able, del, create_time, update_time
        from initializr_account.user_config
        limit #{offset}, #{limit}
    </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryAll" resultMap="UserConfigMap">
    select
    id, key, value,summary,able, del, create_time, update_time
    from initializr_account.user_config
    <where>
      <if test="id != null">
        and id = #{id}
      </if>
      <if test="key != null and key != ''">
        and key = #{key}
      </if>
      <if test="value != null and value != ''">
        and value = #{value}
      </if>
      <if test="able != null">
        and able = #{able}
      </if>
      <if test="del != null">
        and del = #{del}
      </if>
      <if test="createTime != null">
        and create_time = #{createTime}
      </if>
      <if test="updateTime != null">
        and update_time = #{updateTime}
      </if>
    </where>
  </select>

  <!--新增所有列-->
  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into initializr_account.user_config(key, value,summary, able, del, create_time, update_time)
        values (#{key}, #{value},#{summary}, #{able}, #{del}, #{createTime}, #{updateTime})
    </insert>

  <!--通过主键修改数据-->
  <update id="update">
    update initializr_account.user_config
    <set>
      <if test="key != null and key != ''">
        key = #{key},
      </if>
      <if test="value != null and value != ''">
        value = #{value},
      </if>
      <if test="able != null">
        able = #{able},
      </if>
      <if test="del != null">
        del = #{del},
      </if>
      <if test="createTime != null">
        create_time = #{createTime},
      </if>
      <if test="updateTime != null">
        update_time = #{updateTime},
      </if>
    </set>
    where id = #{id}
  </update>

  <!--通过主键删除-->
  <delete id="deleteById">
        delete from initializr_account.user_config where id = #{id}
    </delete>

</mapper>
