<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.account.persistence.dao.UserAccountDao">

  <resultMap id="UserAccountMap" type="pwd.initializr.account.persistence.entity.UserAccountEntity">
    <result column="id" jdbcType="BIGINT" property="id"/>
    <result column="uid" jdbcType="BIGINT" property="uid"/>
    <result column="login_name" jdbcType="VARCHAR" property="loginName"/>
    <result column="login_pwd" jdbcType="VARCHAR" property="loginPwd"/>
    <result column="pwd_time" jdbcType="TIMESTAMP" property="pwdTime"/>
    <result column="type" jdbcType="INTEGER" property="type"/>
    <result column="able" jdbcType="INTEGER" property="able"/>
    <result column="del" jdbcType="INTEGER" property="del"/>
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    <result column="version" jdbcType="BIGINT" property="version"/>
  </resultMap>

  <!--查询单个-->
  <select id="queryById" resultMap="UserAccountMap">
        select
          id, uid, login_name, login_pwd, pwd_time, type, able, del, create_time, update_time, version
        from initializr_account.user_account
        where id = #{id}
    </select>

  <!--登录查询-->
  <select id="queryByLoginNameAndPwd" resultMap="UserAccountMap">
        select
          id, uid, login_name, login_pwd, pwd_time, type, able, del, create_time, update_time
        from initializr_account.user_account
        where login_name = #{loginName} and login_pwd = #{loginPwd}
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="countAllByCondition" resultType="java.lang.Long">
    select count(*) from initializr_account.user_account
    <include refid="countAndQueryAllByCondition"></include>
    limit #{offset}, #{limit}
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryAllByCondition" resultMap="UserAccountMap">
    select
    id, uid, login_name, login_pwd, pwd_time, type, able, del, create_time, update_time
    from initializr_account.user_account
    <include refid="countAndQueryAllByCondition"></include>
    limit #{offset}, #{limit}
  </select>

  <select id="queryAllByUid" resultMap="UserAccountMap">
    select
    id, uid, login_name, login_pwd, pwd_time, type, able, del, create_time, update_time
    from initializr_account.user_account
    where uId = #{uid}
  </select>

  <sql id="countAndQueryAllByCondition">
    <where>
      <if test="userAccountEntity.id != null">
        and id = #{userAccountEntity.id}
      </if>
      <if test="userAccountEntity.uid != null">
        and uid = #{userAccountEntity.uid}
      </if>
      <if test="userAccountEntity.loginName != null and userAccountEntity.loginName != ''">
        and login_name = #{userAccountEntity.loginName}
      </if>
      <if test="userAccountEntity.loginPwd != null and userAccountEntity.loginPwd != ''">
        and login_pwd = #{userAccountEntity.loginPwd}
      </if>
      <if test="pwdTime != null">
        and pwd_time = #{userAccountEntity.pwdTime}
      </if>
      <if test="userAccountEntity.type != null">
        and type = #{userAccountEntity.type}
      </if>
      <if test="userAccountEntity.able != null">
        and able = #{userAccountEntity.able}
      </if>
      <if test="userAccountEntity.del != null">
        and del = #{userAccountEntity.del}
      </if>
      <if test="userAccountEntity.createTime != null">
        and create_time = #{userAccountEntity.createTime}
      </if>
      <if test="userAccountEntity.updateTime != null">
        and update_time = #{userAccountEntity.updateTime}
      </if>
    </where>
  </sql>

  <select id="existName" resultMap="UserAccountMap">
    SELECT login_name from user_account WHERE login_name = #{loginName} AND del = 0
  </select>


  <!--新增所有列-->
  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into initializr_account.user_account(uid, login_name, login_pwd, pwd_time, type, able, del, create_time, update_time)
        values (#{uid}, #{loginName}, #{loginPwd}, #{pwdTime}, #{type}, #{able}, #{del}, #{createTime}, #{updateTime})
  </insert>

  <!--通过主键修改数据-->
  <update id="updatePwd">
    update initializr_account.user_account
    <set>
      <if test="uid != null">
        uid = #{uid},
      </if>
      <if test="loginName != null and loginName != ''">
        login_name = #{loginName},
      </if>
      <if test="loginPwd != null and loginPwd != ''">
        login_pwd = #{loginPwd},
      </if>
      <if test="pwdTime != null">
        pwd_time = #{pwdTime},
      </if>
      <if test="createTime != null">
        create_time = #{createTime},
      </if>
      <if test="updateTime != null">
        update_time = #{updateTime},
      </if>
    </set>
    where id = #{id}
  </update>

  <!--通过主键删除-->
  <delete id="deleteById">
    update initializr_account.user_account set del = 1, update_time = now() where id = #{id}
  </delete>

  <!--通过主键删除-->
  <delete id="deleteByIds">
    update initializr_account.user_account set del = 1, update_time = now() where id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </delete>

  <!--通过主键启禁-->
  <update id="ableById">
    update initializr_account.user_account set able = #{able}, update_time = now() where id = #{id}
  </update>

  <!--通过主键启禁-->
  <update id="ableByIds">
    update initializr_account.user_account set able = #{able}, update_time = now() where id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

  <!--通过用户外键启禁-->
  <update id="ableByUserId">
    update initializr_account.user_account set able = #{able}, update_time = now() where uid = #{userId}
  </update>

  <!--通过用户外键启禁-->
  <update id="ableByUserIds">
    update initializr_account.user_account set able = #{able}, update_time = now() where uid where
    id in
    <foreach close=")" collection="userIds" index="index" item="userId" open="(" separator=",">
      #{userId}
    </foreach>
  </update>

</mapper>
