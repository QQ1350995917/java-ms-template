<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.edu.persistence.dao.EduTermCourseDao">

  <resultMap id="EduTermCourseMap" type="pwd.initializr.edu.persistence.entity.EduTermCourseEntity">
    <result column="id" jdbcType="BIGINT"
            property="id"/>
    <result column="tid" jdbcType="BIGINT"
            property="tid"/>
    <result column="name" jdbcType="VARCHAR"
            property="name"/>
    <result column="order" jdbcType="INTEGER"
            property="order"/>
    <result column="able" jdbcType="INTEGER"
            property="able"/>
    <result column="del" jdbcType="INTEGER"
            property="del"/>
    <result column="create_time" jdbcType="TIMESTAMP"
            property="createTime"/>
    <result column="update_time" jdbcType="TIMESTAMP"
            property="updateTime"/>
  </resultMap>

  <!--根据主键查询查询-->
  <select id="queryById" resultMap="EduTermCourseMap">
    SELECT
      `id`,
      `tid`,
      `name`,
      `order`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      edu_term_course
    WHERE
      id = #{id}
  </select>

  <!--查询指定行数据-->
  <select id="countByCondition" resultType="java.lang.Long">
    SELECT COUNT(*) FROM edu_term_course
    <include
        refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryByCondition" resultMap="EduTermCourseMap">
    SELECT
      `id`,
      `tid`,
      `name`,
      `order`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      edu_term_course
    <include
        refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
    <include
        refid="pwd.initializr.common.web.persistence.entity.SortEntity.entityQuerySort"></include>
    LIMIT
      #{offset}, #{limit}
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryBySql" resultType="java.util.HashMap">
    ${sql}
  </select>

  <!--新增所有列-->
  <insert id="insert" keyProperty="id" useGeneratedKeys="true"
          parameterType="pwd.initializr.edu.persistence.entity.EduTermCourseEntity">
    INSERT INTO edu_term_course
    (
      `id`,
      `tid`,
      `name`,
      `order`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    )
    VALUES
    (
      #{entity.id},
      #{entity.tid},
      #{entity.name},
      #{entity.order},
      #{entity.able},
      #{entity.del},
      #{entity.createTime},
      #{entity.updateTime}
    )
  </insert>

  <!--新增所有列（批量新增）-->
  <insert id="insertByBatch" keyProperty="id" useGeneratedKeys="true"
          parameterType="pwd.initializr.edu.persistence.entity.EduTermCourseEntity">
    INSERT INTO edu_term_course
    (
      `id`,
      `tid`,
      `name`,
      `order`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    )
    VALUES
    <foreach collection="entities" item="entity" separator=",">
    (
      #{entity.id},
      #{entity.tid},
      #{entity.name},
      #{entity.order},
      #{entity.able},
      #{entity.del},
      #{entity.createTime},
      #{entity.updateTime}
    )
    </foreach>
  </insert>

  <!--新增或者替换所有列-->
  <insert id="insertOrReplace" keyProperty="id" useGeneratedKeys="true"
          parameterType="pwd.initializr.edu.persistence.entity.EduTermCourseEntity">
    REPLACE INTO edu_term_course
    (
      `id`,
      `tid`,
      `name`,
      `order`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    )
    VALUES
    (
      #{entity.id},
      #{entity.tid},
      #{entity.name},
      #{entity.order},
      #{entity.able},
      #{entity.del},
      #{entity.createTime},
      #{entity.updateTime}
    )
  </insert>

  <!--新增或者替换所有列（批量新增或者替换）-->
  <insert id="insertOrReplaceByBatch" keyProperty="id" useGeneratedKeys="true"
          parameterType="pwd.initializr.edu.persistence.entity.EduTermCourseEntity">
    REPLACE INTO edu_term_course
    (
      `id`,
      `tid`,
      `name`,
      `order`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    )
    VALUES
    <foreach collection="entities" item="entity" separator=",">
    (
      #{entity.id},
      #{entity.tid},
      #{entity.name},
      #{entity.order},
      #{entity.able},
      #{entity.del},
      #{entity.createTime},
      #{entity.updateTime}
    )
    </foreach>
  </insert>

  <!--通过主键修改数据-->
  <update id="updateById" parameterType="pwd.initializr.edu.persistence.entity.EduTermCourseEntity">
    UPDATE edu_term_course
    <set>
      <if test="entity.id != null">
        `id` = #{entity.id},
      </if>
      <if test="entity.tid != null">
        `tid` = #{entity.tid},
      </if>
      <if test="entity.name != null and entity.name != ''">
        `name` = #{entity.name},
      </if>
      <if test="entity.order != null">
        `order` = #{entity.order},
      </if>
      <if test="entity.able != null">
        `able` = #{entity.able},
      </if>
      <if test="entity.del != null">
        `del` = #{entity.del},
      </if>
      <if test="entity.createTime != null">
        `create_time` = #{entity.createTime},
      </if>
      <if test="entity.updateTime != null">
        `update_time` = #{entity.updateTime},
      </if>
    </set>
    WHERE
      id = #{entity.id}
  </update>

  <!--通过主键删除-->
  <update id="deleteById">
    UPDATE edu_term_course
    SET
      del = 1,
      update_time = #{date}
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量删除-->
  <update id="deleteByIds">
    UPDATE edu_term_course
    SET
      del = 1,
      update_time = #{date}
    WHERE
      id
    IN
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

  <!--通过主键启禁-->
  <update id="ableById">
    UPDATE edu_term_course
    SET
      able = #{able},
      update_time = #{date}
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量启禁-->
  <update id="ableByIds">
    UPDATE edu_term_course
    SET
      able = #{able},
      update_time = #{date}
    WHERE
      id
    IN
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

</mapper>
