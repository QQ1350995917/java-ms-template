<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.edu.persistence.dao.ReadingQuestionDao">

  <resultMap id="ReadingQuestionMap" type="pwd.initializr.edu.persistence.entity.ReadingQuestionEntity">
    <result column="id" jdbcType="BIGINT" property="id"/>
    <result column="pid" jdbcType="BIGINT" property="pid"/>
    <result column="title" jdbcType="VARCHAR" property="title"/>
    <result column="a" jdbcType="VARCHAR" property="a"/>
    <result column="a1" jdbcType="VARCHAR" property="a1"/>
    <result column="a2" jdbcType="VARCHAR" property="a2"/>
    <result column="a3" jdbcType="VARCHAR" property="a3"/>
    <result column="a4" jdbcType="VARCHAR" property="a4"/>
    <result column="a5" jdbcType="VARCHAR" property="a5"/>
    <result column="able" jdbcType="INTEGER" property="able"/>
    <result column="del" jdbcType="INTEGER" property="del"/>
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
  </resultMap>

  <!--根据主键查询查询-->
  <select id="queryById" resultMap="ReadingQuestionMap">
    SELECT
      `id`,
      `pid`,
      `title`,
      `a`,
      `a1`,
      `a2`,
      `a3`,
      `a4`,
      `a5`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      reading_question
    WHERE
      id = #{id}
  </select>

  <!--查询指定行数据-->
  <select id="countByCondition" resultType="java.lang.Long">
    SELECT COUNT(*) FROM reading_question
    <include refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryByCondition" resultMap="ReadingQuestionMap">
    SELECT
      `id`,
      `pid`,
      `title`,
      `a`,
      `a1`,
      `a2`,
      `a3`,
      `a4`,
      `a5`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      reading_question
    <include refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
    <include refid="pwd.initializr.common.web.persistence.entity.SortEntity.entityQuerySort"></include>
    LIMIT
      #{offset}, #{limit}
  </select>

  <!--新增所有列-->
  <insert id="insert" keyProperty="id" useGeneratedKeys="true" parameterType="pwd.initializr.edu.persistence.entity.ReadingQuestionEntity">
    INSERT INTO reading_question
      (
        `id`,
        `pid`,
        `title`,
        `a`,
        `a1`,
        `a2`,
        `a3`,
        `a4`,
        `a5`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
      )
    VALUES
      (
        #{entity.id},
        #{entity.pid},
        #{entity.title},
        #{entity.a},
        #{entity.a1},
        #{entity.a2},
        #{entity.a3},
        #{entity.a4},
        #{entity.a5},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
      )
  </insert>

  <!--新增所有列（批量新增）-->
  <insert id="insertByBatch" keyProperty="id" useGeneratedKeys="true" parameterType="pwd.initializr.edu.persistence.entity.ReadingQuestionEntity">
    INSERT INTO reading_question
    (
        `id`,
        `pid`,
        `title`,
        `a`,
        `a1`,
        `a2`,
        `a3`,
        `a4`,
        `a5`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
    )
    VALUES
    <foreach collection="entities" item="entity" separator=",">
    (
        #{entity.id},
        #{entity.pid},
        #{entity.title},
        #{entity.a},
        #{entity.a1},
        #{entity.a2},
        #{entity.a3},
        #{entity.a4},
        #{entity.a5},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
    )
    </foreach>
  </insert>

  <!--新增或者替换所有列-->
  <insert id="insertOrReplace" keyProperty="id" useGeneratedKeys="true" parameterType="pwd.initializr.edu.persistence.entity.ReadingQuestionEntity">
    REPLACE INTO reading_question
    (
        `id`,
        `pid`,
        `title`,
        `a`,
        `a1`,
        `a2`,
        `a3`,
        `a4`,
        `a5`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
    )
    VALUES
    (
        #{entity.id},
        #{entity.pid},
        #{entity.title},
        #{entity.a},
        #{entity.a1},
        #{entity.a2},
        #{entity.a3},
        #{entity.a4},
        #{entity.a5},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
    )
  </insert>

  <!--新增或者替换所有列（批量新增或者替换）-->
  <insert id="insertOrReplaceByBatch" keyProperty="id" useGeneratedKeys="true" parameterType="pwd.initializr.edu.persistence.entity.ReadingQuestionEntity">
    REPLACE INTO reading_question
    (
        `id`,
        `pid`,
        `title`,
        `a`,
        `a1`,
        `a2`,
        `a3`,
        `a4`,
        `a5`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
    )
    VALUES
    <foreach collection="entities" item="entity" separator=",">
      (
        #{entity.id},
        #{entity.pid},
        #{entity.title},
        #{entity.a},
        #{entity.a1},
        #{entity.a2},
        #{entity.a3},
        #{entity.a4},
        #{entity.a5},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
      )
    </foreach>
  </insert>

  <!--通过主键修改数据-->
  <update id="updateById" parameterType="pwd.initializr.edu.persistence.entity.ReadingQuestionEntity">
    UPDATE reading_question
    <set>
      <if test="entity.id != null and entity.id != ''">
        `id` = #{entity.id},
      </if>
      <if test="entity.pid != null and entity.pid != ''">
        `pid` = #{entity.pid},
      </if>
      <if test="entity.title != null and entity.title != ''">
        `title` = #{entity.title},
      </if>
      <if test="entity.a != null and entity.a != ''">
        `a` = #{entity.a},
      </if>
      <if test="entity.a1 != null and entity.a1 != ''">
        `a1` = #{entity.a1},
      </if>
      <if test="entity.a2 != null and entity.a2 != ''">
        `a2` = #{entity.a2},
      </if>
      <if test="entity.a3 != null and entity.a3 != ''">
        `a3` = #{entity.a3},
      </if>
      <if test="entity.a4 != null and entity.a4 != ''">
        `a4` = #{entity.a4},
      </if>
      <if test="entity.a5 != null and entity.a5 != ''">
        `a5` = #{entity.a5},
      </if>
      <if test="entity.able != null and entity.able != ''">
        `able` = #{entity.able},
      </if>
      <if test="entity.del != null and entity.del != ''">
        `del` = #{entity.del},
      </if>
      <if test="entity.createTime != null">
        `create_time` = #{entity.createTime},
      </if>
      <if test="entity.updateTime != null">
        `update_time` = #{entity.updateTime},
      </if>
    </set>
    WHERE
      id = #{entity.id}
  </update>

  <!--通过主键删除-->
  <update id="deleteById">
    UPDATE reading_question
    SET
      del = 1,
      update_time = #{date}
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量删除-->
  <update id="deleteByIds">
    UPDATE reading_question
    SET
      del = 1,
      update_time = #{date}
    WHERE
      id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

  <!--通过主键启禁-->
  <update id="ableById">
    UPDATE reading_question
    SET
      able = #{able},
      update_time = #{date}
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量启禁-->
  <update id="ableByIds">
    UPDATE reading_question
    SET
      able = #{able},
      update_time = #{date}
    WHERE
      id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

</mapper>
