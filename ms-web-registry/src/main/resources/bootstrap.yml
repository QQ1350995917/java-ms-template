spring:
  profiles:
    active: dev

---
spring:
  profiles: prod
  application:
    name: registry
  cloud:
     config:
       uri: http://localhost:11220
       label: master
       profile: prod


---
spring:
  profiles: dev
  application:
    name: registry
  cloud:
     config:
       uri: http://localhost:11220
       label: master
       profile: dev