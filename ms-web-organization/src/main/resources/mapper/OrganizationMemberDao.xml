<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pwd.initializr.organization.persistence.dao.OrganizationMemberDao">

  <resultMap id="OrganizationMemberMap" type="pwd.initializr.organization.persistence.entity.OrganizationMemberEntity">
    <result column="org_id" jdbcType="BIGINT" property="orgId"/>
    <result column="mem_id" jdbcType="BIGINT" property="memId"/>
    <result column="sort" jdbcType="INTEGER" property="sort"/>
    <result column="able" jdbcType="INTEGER" property="able"/>
    <result column="del" jdbcType="INTEGER" property="del"/>
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
  </resultMap>

  <!--根据主键查询查询-->
  <select id="queryById" resultMap="OrganizationMemberMap">
    SELECT
      `org_id`,
      `mem_id`,
      `sort`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      organization_member
    WHERE
      id = #{id}
  </select>

  <!--查询指定行数据-->
  <select id="countByCondition" resultType="java.lang.Long">
    SELECT COUNT(*) FROM organization_member
    <include refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
  </select>

  <!--通过实体作为筛选条件查询-->
  <select id="queryByCondition" resultMap="OrganizationMemberMap">
    SELECT
      `org_id`,
      `mem_id`,
      `sort`,
      `able`,
      `del`,
      `create_time`,
      `update_time`
    FROM
      organization_member
    <include refid="pwd.initializr.common.web.persistence.entity.ScopeEntity.entityQueryScope"></include>
    <include refid="pwd.initializr.common.web.persistence.entity.SortEntity.entityQuerySort"></include>
    LIMIT
      #{offset}, #{limit}
  </select>

  <!--新增所有列-->
  <insert id="insert" keyProperty="id" useGeneratedKeys="true" parameterType="pwd.initializr.organization.persistence.entity.OrganizationMemberEntity">
    INSERT INTO organization_member
      (
        `org_id`,
        `mem_id`,
        `sort`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
      )
    VALUES
      (
        #{entity.orgId},
        #{entity.memId},
        #{entity.sort},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
      )
  </insert>

  <!--新增所有列（批量新增）-->
  <insert id="insertByBatch" keyProperty="id" useGeneratedKeys="true" parameterType="pwd.initializr.organization.persistence.entity.OrganizationMemberEntity">
    INSERT INTO organization_member
    (
        `org_id`,
        `mem_id`,
        `sort`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
    )
    VALUES
    <foreach collection="entities" item="entity" separator=",">
    (
        #{entity.orgId},
        #{entity.memId},
        #{entity.sort},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
    )
    </foreach>
  </insert>

  <!--新增或者替换所有列-->
  <insert id="insertOrReplace" keyProperty="id" useGeneratedKeys="true" parameterType="pwd.initializr.organization.persistence.entity.OrganizationMemberEntity">
    REPLACE INTO organization_member
    (
        `org_id`,
        `mem_id`,
        `sort`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
    )
    VALUES
    (
        #{entity.orgId},
        #{entity.memId},
        #{entity.sort},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
    )
  </insert>

  <!--新增或者替换所有列（批量新增或者替换）-->
  <insert id="insertOrReplaceByBatch" keyProperty="id" useGeneratedKeys="true" parameterType="pwd.initializr.organization.persistence.entity.OrganizationMemberEntity">
    REPLACE INTO organization_member
    (
        `org_id`,
        `mem_id`,
        `sort`,
        `able`,
        `del`,
        `create_time`,
        `update_time`
    )
    VALUES
    <foreach collection="entities" item="entity" separator=",">
      (
        #{entity.orgId},
        #{entity.memId},
        #{entity.sort},
        #{entity.able},
        #{entity.del},
        #{entity.createTime},
        #{entity.updateTime}
      )
    </foreach>
  </insert>

  <!--通过主键修改数据-->
  <update id="updateById" parameterType="pwd.initializr.organization.persistence.entity.OrganizationMemberEntity">
    UPDATE organization_member
    <set>
      <if test="entity.orgId != null">
        `org_id` = #{entity.orgId},
      </if>
      <if test="entity.memId != null">
        `mem_id` = #{entity.memId},
      </if>
      <if test="entity.sort != null">
        `sort` = #{entity.sort},
      </if>
      <if test="entity.able != null">
        `able` = #{entity.able},
      </if>
      <if test="entity.del != null">
        `del` = #{entity.del},
      </if>
      <if test="entity.createTime != null">
        `create_time` = #{entity.createTime},
      </if>
      <if test="entity.updateTime != null">
        `update_time` = #{entity.updateTime},
      </if>
    </set>
    WHERE
      id = #{entity.id}
  </update>

  <!--通过主键删除-->
  <update id="deleteById">
    UPDATE organization_member
    SET
      del = 1,
      update_time = #{date}
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量删除-->
  <update id="deleteByIds">
    UPDATE organization_member
    SET
      del = 1,
      update_time = #{date}
    WHERE
      id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

  <!--通过主键启禁-->
  <update id="ableById">
    UPDATE organization_member
    SET
      able = #{able},
      update_time = #{date}
    WHERE
      id = #{id}
  </update>

  <!--通过主键批量启禁-->
  <update id="ableByIds">
    UPDATE organization_member
    SET
      able = #{able},
      update_time = #{date}
    WHERE
      id in
    <foreach close=")" collection="ids" index="index" item="id" open="(" separator=",">
      #{id}
    </foreach>
  </update>

</mapper>
